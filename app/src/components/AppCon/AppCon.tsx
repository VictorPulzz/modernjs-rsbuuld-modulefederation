import React, {FC} from "react";

interface AppConProps {
};
import styles from "./AppCon.module.scss";
export const AppCon: FC<AppConProps> = ({}) => {
  return (
    <div className={styles.too}>
11112щл321л1л2л
    </div>
  );
};
